# xdv-edx Docs

Documentation for the `xdv-edx` editor subsystem.

## Files

- `editor_flow.md`: startup lifecycle, run loop, and command path.
- `module_reference.md`: source-module responsibilities.
- `keybindings.md`: key handling and command mappings.

Use these docs with `../README.md` and `../State.toml` for integration context.
