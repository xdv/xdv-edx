# xdv-edx Docs

Documentation for the `xdv-edx` editor subsystem.

## Files

- `editor_flow.md`: startup lifecycle, deterministic loop model, render and file-flow.
- `module_reference.md`: source-module responsibilities and test modules.
- `keybindings.md`: key classes, keybindings, and keypath helpers.

Use these docs with `../README.md` and `../State.toml` for integration context.
